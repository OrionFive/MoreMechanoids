using RimWorld;

namespace MoreMechanoids;

public class CompProperties_EmpShield : CompProperties_Shield
{
    public CompProperties_EmpShield()
    {
        compClass = typeof(CompEmpShield);
    }
}